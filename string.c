#include <stdio.h>

int main ()
{
    // 数组 初始化 
    char test[4];
   char greeting[6] = {'H', 'e', 'l', 'l', 'o'};
    printf(test);
   printf("Greeting message: %s\n", greeting );

printf(greeting);


   return 0;
}